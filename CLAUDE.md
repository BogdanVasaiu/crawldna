# CLAUDE.md
> Auto-generated by repoDNA · Smart Update · Model: `gpt-oss:120b-cloud`
> Generated: 2026-06-15T20:51:51.333Z

---

## Project Overview

 docdna is a task-driven web crawler that turns websites into clean Markdown.

You give it one or more links plus a plain-English task — e.g. "Extract all documentation", "Extract the pizza menu", "Get the pricing". For each site it:

Opens every page in a real browser (so JavaScript-rendered sites and SPAs work, not just static HTML).
Reveals hidden content — automatically clicks tabs, expands accordions, presses "load more", scrolls for lazy content — so it captures things a normal scraper never sees (like the pnpm/yarn/npm/bun install commands behind tabs).
Uses AI to stay on-task — it keeps only the sections that match your task and decides which links are worth following (e.g. follow docs, skip blog/marketing).

---

## File Tree

```
├── bin/
│   └── cli.mjs
├── src/
│   ├── engine/
│   │   ├── actions.mjs
│   │   ├── crawl-page.mjs
│   │   ├── decide.mjs
│   │   ├── perceive.mjs
│   │   └── reveal.mjs
│   ├── lib/
│   │   ├── browser.mjs
│   │   ├── fetcher.mjs
│   │   ├── layout.mjs
│   │   ├── output.mjs
│   │   ├── pool.mjs
│   │   ├── runs.mjs
│   │   ├── task.mjs
│   │   └── url.mjs
│   ├── profiles/
│   │   ├── docs/
│   │   │   ├── framework/
│   │   │   │   ├── docusaurus.mjs
│   │   │   │   ├── generic.mjs
│   │   │   │   ├── index.mjs
│   │   │   │   ├── mkdocs.mjs
│   │   │   │   └── vitepress.mjs
│   │   │   ├── llms.mjs
│   │   │   └── sitemap.mjs
│   │   └── docs.mjs
│   ├── extract.mjs
│   └── index.mjs
├── ui/
│   ├── index.html
│   └── server.mjs
├── .gitignore
├── build-spec.md
├── CLAUDE.md
├── LICENSE
├── package-lock.json
├── package.json
└── README.md
```

---

## Dependency Graph

> Auto-extracted from static import analysis.

**`src/engine/crawl-page.mjs`** imports: `../lib/browser.mjs`, `../lib/fetcher.mjs`, `../extract.mjs`, `./reveal.mjs`, `./decide.mjs`, `../lib/task.mjs`, `../lib/url.mjs`
**`src/index.mjs`** imports: `./profiles/docs.mjs`, `./engine/crawl-page.mjs`, `./lib/layout.mjs`, `./lib/runs.mjs`, `./lib/browser.mjs`, `./lib/url.mjs`, `./lib/task.mjs`
**`src/profiles/docs/framework/index.mjs`** imports: `./docusaurus.mjs`, `./vitepress.mjs`, `./mkdocs.mjs`, `./generic.mjs`, `../../../lib/url.mjs`
**`src/engine/reveal.mjs`** imports: `./perceive.mjs`, `./actions.mjs`, `../extract.mjs`
**`bin/cli.mjs`** imports: `../src/index.mjs`, `../src/lib/runs.mjs`, `../ui/server.mjs`
**`src/profiles/docs.mjs`** imports: `./docs/llms.mjs`, `./docs/sitemap.mjs`, `../lib/url.mjs`
**`ui/server.mjs`** imports: `../src/index.mjs`, `../src/lib/browser.mjs`, `../src/lib/runs.mjs`
**`src/profiles/docs/llms.mjs`** imports: `../../lib/fetcher.mjs`, `../../lib/url.mjs`
**`src/profiles/docs/sitemap.mjs`** imports: `../../lib/fetcher.mjs`, `../../lib/url.mjs`
**`src/lib/layout.mjs`** imports: `./url.mjs`, `../engine/decide.mjs`
**`src/lib/fetcher.mjs`** imports: `./browser.mjs`
**`src/lib/runs.mjs`** imports: `./output.mjs`

### Most imported modules
- `../extract.mjs` ← used by 2 files
- `../../lib/fetcher.mjs` ← used by 2 files
- `../../lib/url.mjs` ← used by 2 files
- `../src/index.mjs` ← used by 2 files
- `../src/lib/runs.mjs` ← used by 2 files
- `../lib/url.mjs` ← used by 2 files
- `./perceive.mjs` ← used by 1 file
- `./actions.mjs` ← used by 1 file

---
 
## Source Code

### `src/engine/actions.mjs`
**Role:** Provides low‑level actions that manipulate a Playwright page to reveal hidden DOM fragments and to scroll for lazy‑loaded content, using the `data-docdna-id` attribute as the selector key.  
**Key exports:**  
- `clickRevealer(page, id)` → `Promise<{ ok: boolean, navigatedTo?: string, note?: string }>` – clicks the element with the given id, handles possible navigation, and returns a status object.  
- `scrollStep(page)` → `Promise<boolean>` – scrolls the viewport by roughly one viewport height and reports whether the document height grew.  
**Non-obvious deps:** Relies on Playwright’s `Page` API methods (`locator`, `scrollIntoViewIfNeeded`, `click`, `waitForTimeout`, `url`, `goBack`, `evaluate`).  
**Side effects:** `clickRevealer` may trigger a navigation away from the current URL, then performs a back navigation; both functions scroll the page and can cause network requests for lazy‑loaded resources.  
**Architecture:** Does not import any other project modules; the two exported functions constitute the stable public API of this module; no internal helpers or singleton state are defined.  
**Watch out:** `clickRevealer` expects a unique element with the specified `data-docdna-id` and assumes a history entry exists for `goBack`; `scrollStep` interprets any increase of more than four pixels in `document.body.scrollHeight` as new content, which can be affected by minor layout changes; both functions catch and suppress errors, so callers must handle failure based on the returned boolean or status object rather than exceptions.

---

### `src/engine/reveal.mjs`
**Role:** This file implements the reveal engine that systematically interacts with every UI control capable of hiding content (tabs, accordions, “load more”, etc.) on a Playwright page. It coordinates perception, action execution, and markdown accumulation to return a complete snapshot of the page’s visible content and navigation targets.  

**Key exports:**  
- `revealAll(page, ctx, url) → Promise<{ markdown, title, links, navLinks, routes, hitCap }>`  

**Non-obvious deps:** none  

**Side effects:** Invokes Playwright page methods (`content`, `url`, DOM clicks, scrolling), emits structured events through `ctx.emit`, may cause navigation when `clickRevealer` returns a `navigatedTo` URL, and populates a `BlockAccumulator` instance with extracted markdown.  

**Architecture:** Directly imports `perceive` from `./perceive.mjs`, `clickRevealer` and `scrollStep` from `./actions.mjs`, and `extractMarkdown` plus `BlockAccumulator` from `../extract.mjs`. No global singletons are created; the only exported symbol is `revealAll`, while `capture` and local sets/maps are internal helpers used solely within the function.  

**Watch out:** The function respects a configurable action cap (`ctx.options.maxActions` or default 40, minimum 8) and relies on the `actioned` set to avoid re‑clicking the same control. The “load more” loop is bounded to 40 iterations and increments the global `actions` counter, so exceeding `maxActions` will terminate the crawl early and set `hitCap`. The `scrolledOut` flag assumes `scrollStep` returns a truthy value when new content appears; if the page implements infinite scroll without a terminating condition, the loop may repeatedly call `scrollStep` without progress. `ctx.emit` must accept the event shape used in the file, and `ctx.shouldStop` must be callable to allow early termination.

---

### `src/extract.mjs`
**Role:** This module isolates the primary article element from a raw HTML string, removes navigation and advertisement chrome, rewrites relative URLs to absolute form, and converts the cleaned HTML into GitHub‑Flavored Markdown. It also provides utilities for splitting Markdown into logical blocks and for de‑duplicating those blocks across multiple page states.

**Key exports:**
- `extractMarkdown(html, {contentSelector, baseUrl, title})` → `{ title: string, markdown: string }` – parses HTML, selects the densest content node, strips chrome, absolutizes links, and returns cleaned Markdown with a derived or overridden title.  
- `splitBlocks(markdown)` → `string[]` – breaks a Markdown document into paragraphs, headings, lists, tables, and fenced code sections without splitting inside code fences.  
- `BlockAccumulator` – class that accumulates unique Markdown blocks; `add(markdown, {label})` returns the number of newly added blocks, `size()` returns the total count, and `toMarkdown()` emits the blocks in insertion order with optional variant comments.

**Non-obvious deps:** `turndown` (TurndownService) together with `turndown-plugin-gfm` for GFM table support; `node-html-parser` for tolerant HTML parsing; `node:crypto` for SHA‑1 hashing of normalized blocks.

**Side effects:** none

**Architecture:** imports `createHash` from `node:crypto`, `parse` from `node-html-parser`, `TurndownService` from `turndown`, and `gfm` from `turndown-plugin-gfm`; does not import any project‑internal modules; defines internal helpers (`buildTurndown`, `textOf`, `absolutize`, `pickMainContent`, `normalizeBlock`) that are not exported; exports the three public APIs listed above while the helper functions remain private to this file.

**Watch out:** `extractMarkdown` relies on the length of text content (> 40 characters) to choose a main node, so pages with very short but meaningful sections may be missed; the regular expressions that strip toolbar links and ad anchors are broad and can unintentionally remove legitimate links that match the patterns; `splitBlocks` treats any line starting with three backticks or tildes as a fence delimiter, so unconventional fence markers may cause mis‑splitting; `BlockAccumulator` hashes a whitespace‑collapsed, lower‑cased version of each block, which can cause distinct blocks that differ only in case or spacing to be considered duplicates.

---

### `src/lib/browser.mjs`
**Role:** This module lazily imports the optional Playwright package, creates and caches a single headless Chromium browser for the entire application, and supplies helpers to probe browser availability, install missing Chromium binaries, and generate isolated pages equipped with a click‑listener sniffer. It centralizes all user‑facing error messages related to Playwright installation and browser launch.

**Key exports:**
- `browserError()` – returns the current user‑friendly error string or `null` if no error is recorded.  
- `getBrowser()` – async; launches Chromium once (if not already launched) and returns the shared Playwright `Browser` instance.  
- `isBrowserAvailable()` – async; returns `true` if a browser can be launched, `false` otherwise, without throwing.  
- `newPage()` – async; obtains the shared browser, creates a fresh `BrowserContext` with a custom user‑agent and viewport, injects the SNIFFER script, opens a new `Page`, and returns `{ page, context }`.  
- `closeBrowser()` – async; closes the cached browser if it exists and clears the internal reference.  
- `ensureBrowser({ log = () => {} } = {})` – async; checks availability, runs a one‑time `npx playwright install chromium` subprocess if needed, logs progress via the supplied `log` callback, and finally returns `true` when the browser is ready.

**Non-obvious deps:** Dynamically imports the optional `playwright` npm package; dynamically imports Node’s `child_process` module to spawn `npx playwright install chromium` during automatic installation.

**Side effects:** Mutates module‑level variables (`_pw`, `_imported`, `_browser`, `_error`, `_installTried`); launches a Chromium process that persists across calls; creates new browser contexts and pages with an injected init script; spawns a child process that writes installation logs to the parent’s stdio; may close the shared browser on `closeBrowser`.

**Architecture:** No internal project modules are imported; the file owns the singleton state for Playwright (`_pw`), the shared browser (`_browser`), and error tracking (`_error`, `_installTried`). Exported functions (`browserError`, `getBrowser`, `isBrowserAvailable`, `newPage`, `closeBrowser`, `ensureBrowser`) constitute the stable public API; helper functions (`importPlaywright`, `cleanLaunchError`) and the `SNIFFER` script are internal and not exported.

**Watch out:** `ensureBrowser` will attempt the Chromium installation only once per process (`_installTried` flag); concurrent calls to `getBrowser` before the first launch completes may race because the launch promise is not stored; `isBrowserAvailable` deliberately does not cache a launch failure so that a later installation can be detected; the injected SNIFFER script runs in every new page context and silently swallows errors, so failures inside it are not reported.

---

### `src/lib/fetcher.mjs`
**Role:** Provides a network abstraction that fetches raw HTML via `fetch` and optionally escalates to a Playwright browser when the content appears to be a JavaScript‑rendered shell or when the caller forces browser rendering.

**Key exports:**
fetchText(url, { headers, timeout = 30000, accept } = {}) – returns a promise resolving to `{ ok, status, text, contentType, finalUrl, error? }`.  
looksRendered(html) – returns a boolean indicating whether the HTML contains enough visible text to be considered rendered.  
loadHtml(url, { browserMode = 'auto', ctx } = {}) – returns a promise resolving to `{ html, finalUrl, status, rendered, error? }`.

**Non-obvious deps:** none

**Side effects:** Performs network requests (fetch and Playwright navigation); may launch a Playwright browser page; may emit a warning via `ctx.emit` when browser mode “always” is requested but Playwright is unavailable.

**Architecture:** Imports `isBrowserAvailable` and `newPage` from `./browser.mjs`. Exports three public functions (`fetchText`, `looksRendered`, `loadHtml`). No global state is created; all behavior is driven by parameters and the imported browser helpers.

**Watch out:** `loadHtml` falls back to a plain fetch when the browser is unavailable or when the fetched HTML passes the `looksRendered` heuristic; ensure the heuristic threshold (800 characters) matches your content expectations. The function expects `ctx.emit` to be a callable if provided; passing a non‑function may cause a runtime error. Closing the Playwright context is mandatory to avoid resource leaks.

---

### `src/lib/pool.mjs`
**Role:** This file provides a tiny fixed‑size concurrency pool that processes an array of items with a bounded number of parallel workers. It guarantees that the returned array preserves the input order and that each item’s error is captured as an object `{ __error }` instead of aborting the whole operation. It also allows an optional `shouldStop` callback to halt processing gracefully between items.

**Key exports:**
- `mapPool(items, concurrency, fn, { shouldStop })` — async function that returns a `Promise<Array<any>>` where each element is either the value returned by `fn` or an object `{ __error }` if `fn` threw; results are ordered to match `items`.

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** 
- Imports from this project: none  
- Global state or singletons owned: none  
- Stable public API: `mapPool`  
- Internal helpers: none

**Watch out:** The `concurrency` argument is clamped to at least 1 and at most the length of `items`; callers must handle the `{ __error }` wrapper to detect failures. The optional `shouldStop` function is evaluated before each item, so a true return stops further work but does not cancel work already in flight.

---

### `src/lib/task.mjs`
**Role:** Exports a predicate that determines if a task string matches documentation‑related keywords, enabling the orchestrator and engine to route docs extraction tasks.  
**Exports:** isDocsTask

---

### `src/profiles/docs/framework/docusaurus.mjs`
**Role:** This file defines the Docusaurus profile object that detects Docusaurus sites and provides the CSS selectors used to locate documentation content and sidebar navigation for the crawler.

**Key exports:**  
- match(root, html) — inspects the provided DOM root and raw HTML, returning a boolean that is true when Docusaurus‑specific markers are found.

**Non-obvious deps:** none

**Side effects:** none

**Architecture:**  
- Exports a default object containing `name`, `match`, `contentSelector`, and `navSelector`; no internal imports are performed.  
- The `match` function is the only callable member exposed as part of the public API; `contentSelector` and `navSelector` are static arrays consumed by the extraction engine to query the page.

**Watch out:**  
- The `match` implementation checks for class names such as `.theme-doc-markdown` and `.menu__list`; if another site uses identical class names, the profile may incorrectly identify it as Docusaurus. Adjust selectors if false positives arise.

---

### `src/profiles/docs/framework/generic.mjs`
**Role:** This file defines the generic documentation profile that the extractor falls back to when it cannot recognize a specific docs generator. It provides a constant `name`, an unconditional `match()` method, and default CSS selectors for locating main content and navigation elements.

**Key exports:**
- `default` — fallback profile object containing `name`, `match()`, `contentSelector`, and `navSelector`.

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** The module imports nothing from the project and does not instantiate any global objects. It solely exports the default profile object, which constitutes the stable public API; there are no internal helper exports.

**Watch out:** none

---

### `src/profiles/docs/framework/index.mjs`
**Role:** Detects which documentation generator a page is built with and supplies a framework‑specific configuration for content extraction. Provides a helper that harvests absolute navigation URLs from the rendered navigation tree of the detected framework.

**Key exports:**
- `detectFramework(html)` – parses the HTML and returns the first matching framework config object (one of the imported modules or the generic fallback).
- `navLinks(html, baseUrl, fw)` – returns an array of deduped absolute URLs found in the navigation selectors of the supplied framework config.

**Non-obvious deps:** `node-html-parser` `parse` (parsing behavior can vary between library versions); `resolveUrl` from `../../../lib/url.mjs` (relies on its URL‑resolution algorithm); framework modules `docusaurus.mjs`, `vitepress.mjs`, `mkdocs.mjs`, `generic.mjs` (each must export a `match` function and optional selector arrays).

**Side effects:** none

**Architecture:** Directly imports `parse`, the four framework modules, and `resolveUrl`. Does not create or modify any global state. Exports `detectFramework` and `navLinks` as the stable public API; the `INLINE` array and `FRAMEWORKS` list are internal implementation details.

**Watch out:** The detection order in `FRAMEWORKS` determines which framework wins when multiple matchers succeed; reordering can change results. Any added framework object must implement a `match(root, html)` function and may provide a `navSelector` array, otherwise `navLinks` will fall back to scanning the whole document. `navLinks` silently skips `href`s that begin with `#`, `mailto:`, `tel:`, or `javascript:` and discards any link that `resolveUrl` cannot turn into an absolute URL.

---

### `src/profiles/docs/framework/mkdocs.mjs`
**Role:** Provides the MkDocs documentation profile that identifies MkDocs sites and supplies the CSS selectors used by the crawler to extract page content and navigation.  
**Key exports:**  
- `name` — string `'mkdocs'` identifying this profile.  
- `match(root, html)` — returns a boolean true when the supplied HTML or DOM contains MkDocs‑specific markers.  
- `contentSelector` — array of CSS selectors (`['.md-content article', '.md-content__inner', '[role=main]', 'article']`) that locate the main article element.  
- `navSelector` — array of CSS selectors (`['.md-nav--primary', '.md-nav', 'nav.md-nav']`) that locate the primary navigation block.  
**Non-obvious deps:** none  
**Side effects:** none  
**Architecture:** imports no modules from the project; does not create or modify any global state; the default exported object is the stable public API consumed by the crawling engine, while the listed properties serve as its only interface.  
**Watch out:** `match` expects a valid `root` element and an `html` string—passing null or undefined will cause a runtime error; the selector arrays assume the default Material for MkDocs theme structure and may need revision if the theme is customized.

---

### `src/profiles/docs/framework/vitepress.mjs`
**Role:** Provides the VitePress documentation profile, defining how docdna recognizes a VitePress site and which DOM selectors to use for extracting main content and navigation.  
**Key exports:**
- `default` — object with `name`, `match(root, html)`, `contentSelector`, and `navSelector` used by the crawler to handle VitePress sites.  
**Non-obvious deps:** none  
**Side effects:** none  
**Architecture:** imports nothing from the project; exports a single default object that constitutes a stable public API; no global state or singletons are created.  
**Watch out:** `match` assumes `root` is a non‑null Element and `html` is a string; passing undefined or a non‑Element will cause a runtime error.

---

### `src/profiles/docs/llms.mjs`
**Role:** Parses a `/llms-full.txt` file when it exists at the origin of a given base URL, splits the file into markdown sections, extracts or synthesizes a source URL for each section, and returns a structured representation of those pages.  
**Key exports:**  
- `tryLlmsFull(baseUrl)` → `Promise<null | { sourceUrl: string, pages: Array<{ url: string, title: string, markdown: string }> }>`  

**Non-obvious deps:** `fetchText`, `originOf`, `slug`  

**Side effects:** Issues an HTTP GET request to `${origin}/llms-full.txt` during execution of `tryLlmsFull`.  

**Architecture:** Imports `fetchText` from `../../lib/fetcher.mjs` and `originOf`, `slug` from `../../lib/url.mjs`; defines internal helpers `looksLikeHtml`, `findSource`, and `splitOnHeading`; exports only `tryLlmsFull` as the stable public API while the helpers remain private.  

**Watch out:** `baseUrl` must yield a non‑null origin or the function returns `null`; the fetched file is rejected if its length is under 500 characters or if it begins with HTML markup; `findSource` only scans the first six lines for a `Source:`/`URL:` marker, so markers later in the section are ignored; when no explicit source is found, the URL falls back to `origin/#${slug(title)}`, which may not be unique.

---

### `src/profiles/docs/sitemap.mjs`
**Role:** This module enumerates all URLs listed in a site's sitemaps for the documentation profile. It resolves standard sitemap locations, extracts additional sitemap URLs from `robots.txt`, and recursively follows sitemap indexes to collect page URLs.  

**Key exports:**  
- `collectSitemapUrls(baseUrl, { shouldStop, maxDepth }) → Promise<string[]>` – returns a deduplicated list of URLs discovered in the site's sitemaps.  

**Non-obvious deps:**  
- `fast-xml-parser` (provides `XMLParser`) – parsing rules may differ between library versions.  

**Side effects:**  
- Issues HTTP GET requests for sitemap files and `robots.txt` via `fetchText`.  

**Architecture:**  
- Imports `fetchText` from `../../lib/fetcher.mjs`, `XMLParser` from `fast-xml-parser`, and `originOf` from `../../lib/url.mjs`.  
- Does not create or modify global state; only `collectSitemapUrls` is exported as the stable public API, while `toArray` and the inner `walk` function remain private helpers.  

**Watch out:**  
- Recursion halts when `depth` exceeds `maxDepth` (default 4); deeper sitemap indexes are ignored.  
- The optional `shouldStop` callback is evaluated before each request and can abort the crawl early.  
- Non‑200 responses or XML parsing failures are silently ignored, which may cause missing URLs without raising an error.

---

### `bin/cli.mjs`
**Role:** This file implements the docdna command‑line interface. It parses arguments, builds crawl targets, maps flags to core options, invokes the core `crawlDocs` engine, renders streaming events to the terminal, and provides the `serve` and `runs` sub‑commands for the web UI and cache management.

**Key exports:** none

**Non-obvious deps:** `../ui/server.mjs` – dynamically imported only when the `serve` sub‑command is executed; the import supplies `startServer` which launches the web UI.

**Side effects:** Writes progress, warnings and results to `process.stdout`/`process.stderr`; registers a `SIGINT` listener to stop a running crawl; triggers file system writes through `crawlDocs` and the runs‑cache helpers; may change process exit code.

**Architecture:** Direct imports from the project: `crawlDocs`, `DEFAULT_OPTIONS` (src/index.mjs); `listRuns`, `deleteRun`, `deleteAllRuns`, `cacheRoot` (src/lib/runs.mjs). It owns the CLI parsing, target construction, flag conversion, event rendering, and the top‑level `main` orchestration. All functions (`buildTargets`, `optionsFromFlags`, `renderEvent`, `runCrawl`, `fmtDate`, `runsCommand`, `main`) are internal helpers; the file exports nothing publicly.

**Watch out:** `buildTargets` assumes the number of `--task` values aligns with `--url` values; mismatches yield undefined tasks. `optionsFromFlags` converts flag strings to numbers without validation, so non‑numeric input can produce `NaN`. The `SIGINT` handler is attached before the crawl and removed after; forgetting to remove it could affect subsequent runs. The dynamic import path must remain correct relative to this file for the `serve` command.

---

### `src/engine/crawl-page.mjs`
**Role:** This file implements the per‑page crawling engine that renders a URL in a Playwright browser, exhaustively reveals hidden or dynamically loaded content, optionally scopes the extracted Markdown to a custom task, and returns the discovered in‑scope links. It also provides a static‑HTML fallback path that is used when a browser cannot be launched.

**Key exports:**  
crawlPageWithEngine(target, ctx) — returns a Promise resolving to `{ page: object|null, links: string[] }` where `page` contains the URL, task, title, Markdown, and metadata, and `links` is the list of URLs selected for further crawling.

**Non-obvious deps:**  
Playwright (accessed via `newPage` from `../lib/browser.mjs`) – relies on version‑specific page lifecycle methods such as `popup`, `waitForLoadState`, and `close` to manage browser contexts correctly.

**Side effects:**  
Opens a new Playwright page and browser context for each call.  
Registers a `'popup'` event listener that records and immediately closes popup windows.  
Emits warning or error events through `ctx.emit`.  
Performs network requests via `page.goto` and via `loadHtml` in the static fallback.  
Closes the Playwright context after rendering.  
Invokes AI model calls (`aiSelectLinks`, `aiScopeContent`) using the model name from `ctx.options.model`.

**Architecture:**  
Imports from the project: `isBrowserAvailable`, `newPage`, `browserError` (lib/browser.mjs); `loadHtml` (lib/fetcher.mjs); `extractMarkdown` (extract.mjs); `revealAll` (engine/reveal.mjs); `aiScopeContent`, `aiSelectLinks` (engine/decide.mjs); `isDocsTask` (lib/task.mjs); `normalizeUrl`, `inScope`, `resolveUrl` (lib/url.mjs).  
Initializes per‑run link‑follow cache on `ctx._followCache` and a one‑time browser‑warning flag on `ctx._browserWarned`.  
Exports the stable public API `crawlPageWithEngine`; internal helpers `decideFollow`, `inScopeUnique`, and `staticFallback` remain private to the module.

**Watch out:**  
The follow‑cache stored on `ctx._followCache` does not differentiate between tasks, so a link judged irrelevant for one task may be omitted for another when the same `ctx` is reused.  
The engine assumes `ctx.emit` is a callable function; providing a context without this method will cause a runtime exception.  
The popup listener closes every popup immediately, which can prevent pages that rely on popup content from being processed correctly.  
The reveal‑cap warning uses `ctx.options.maxActions` at the time of the call; changing the option after a page has been opened does not affect the already‑computed cap.

---

### `src/engine/decide.mjs`
**Role:** This file owns the AI‑driven judgment layer that decides which page sections to keep, which links to follow, and how extracted items should be grouped into Markdown files. It centralises all prompts sent to the Ollama model and all parsing of the model’s JSON replies.

**Key exports:**  
`aiScopeContent({ model, task, title, markdown })` – returns `Promise<{ markdown: string, relevant: boolean }>`  
`aiSelectLinks({ model, task, links })` – returns `Promise<string[]>`  
`aiPlanLayout({ model, task, items })` – returns `Promise<{ perPage: true } | { files: Array<{ filename: string, items: number[] }> } | null>`

**Non-obvious deps:**  
`ollama` – the npm package that provides a `chat` method used to send prompts to a locally running Ollama model; the method returns an object whose `message.content` field contains the model’s reply.

**Side effects:**  
Invokes `ollama.chat`, which issues a network request to the Ollama server; the request is asynchronous and may affect external I/O latency. No other global state is mutated.

**Architecture:**  
- Imports only the external `ollama` package.  
- Defines internal helpers `chat`, `parseJson`, and `sectionize`; these helpers are used exclusively by the exported functions and are not exported themselves.  
- Exports `aiScopeContent`, `aiSelectLinks`, and `aiPlanLayout` as the stable public API; the three helpers remain private implementation details.  
- Does not import any other project modules.

**Watch out:**  
- `aiScopeContent` skips processing when `markdown` is falsy or shorter than 1,200 characters, returning the input unchanged.  
- When a page yields a single section, the function only asks the model whether the whole page is relevant; a negative JSON reply discards the page entirely.  
- `aiSelectLinks` truncates the candidate list to the first 160 links; if the model’s JSON cannot be parsed, it falls back to returning all truncated links.  
- `aiPlanLayout` returns `null` for an empty `items` array or when the model’s reply cannot be parsed.  
- `parseJson` extracts the first JSON block it finds; malformed or missing JSON leads to a `null` result, triggering the fallback logic in the callers.  
- An empty `keep` set in `aiScopeContent` is treated as “keep everything” rather than discarding the page.

---

### `src/engine/perceive.mjs`
**Role:** Extracts a structured snapshot of the main content area of a Playwright page, discovers interactive elements that can reveal hidden documentation, collects in‑page links and script‑derived routes, and generates a short fingerprint for change detection.  

**Key exports:**  
- `perceive(page, { maxText = 2500, maxRevealers = 150, maxLinks = 400 } = {})` → `Promise<object>` – returns an object containing `url`, `title`, trimmed `mainText`, arrays `revealers`, `consent`, `links`, `routes`, a `hiddenCount`, `scrollHeight`, and a SHA‑1‑based `fingerprint`.  

**Non-obvious deps:**  
- `node:crypto` (`createHash`) – used to compute the SHA‑1 fingerprint of the page snapshot; behavior can vary with Node version.  

**Side effects:**  
- Adds a `data-docdna-id` attribute to every consent button and every element classified as a revealer inside the page’s DOM.  
- Reads layout properties such as `scrollHeight` and element bounding rectangles but does not perform network I/O.  

**Architecture:**  
- Imports: `createHash` from `node:crypto`.  
- No imports from other project modules.  
- Internal helper: `fingerprintOf(data)` (private, not exported).  
- Public API: `perceive` (stable export). All other functions remain internal.  

**Watch out:**  
- Because `perceive` mutates the page by annotating elements, calling it multiple times on the same `page` instance will see previously added `data-docdna-id` attributes, potentially inflating `revealers` and `consent` counts.  
- The `maxRevealers` and `maxLinks` caps silently truncate the corresponding arrays; callers must be aware that results may be incomplete when those limits are reached.  
- Route extraction relies on a regex over inline script text; it may capture strings that look like paths but are not actual navigation routes, so `routes` should be treated as best‑effort data.

---

### `src/index.mjs`
**Role:** Provides the public API for docdna, orchestrating the end‑to‑end crawl workflow, exposing configuration defaults, target normalisation, and the `crawlDocs` entry point that returns an async‑iterable run object.  

**Key exports:**  
- `DEFAULT_OPTIONS` – the baseline options object used when a caller omits settings.  
- `normalizeTargets(targets, defaultTask)` – converts a mixed‑type `targets` argument into an array of `{ url, task }` objects, discarding entries that are not strings or objects with a `url`.  
- `crawlDocs(targets, options = {})` – launches a crawl according to the supplied targets and options; returns an object that implements `[Symbol.asyncIterator]()` for event streaming, a `result` getter that resolves to the final `Result`, and a `stop()` method to abort the run.  
- `default` – alias for `crawlDocs` (default export).  

**Non-obvious deps:** none  

**Side effects:**  
- Invokes the Playwright‑based engine (`crawlPageWithEngine`) which opens browser contexts and performs network requests.  
- Calls `saveRun` and `planFiles` to write the crawl output and metadata to the on‑disk cache.  
- Calls `closeBrowser` to shut down the shared browser singleton after the run finishes.  
- Emits events to the caller‑provided `onEvent` callback and to the internal async‑iterator stream.  

**Architecture:**  
- Imports internal modules: `runDocsProfile` (profiles/docs.mjs), `crawlPageWithEngine` (engine/crawl-page.mjs), `planFiles` (lib/layout.mjs), `saveRun` (lib/runs.mjs), `closeBrowser` (lib/browser.mjs), URL helpers (`normalizeUrl`, `inScope`, `pathOf`, `originOf` from lib/url.mjs), and `isDocsTask` (lib/task.mjs).  
- Uses Node’s `crypto.createHash` to compute SHA‑1 signatures for duplicate‑page detection.  
- Owns per‑run state: an event stream, a mutable `result` object, a `Set` of content hashes, a `Promise` that resolves to the final result, and a `ctx` context object that carries options, progress counters, and helper methods (`emit`, `shouldStop`, `addPage`, `runEngine`).  
- Exposes `DEFAULT_OPTIONS`, `normalizeTargets`, and `crawlDocs` (named and default) as the stable public API; `createEventStream` and `runGeneralCrawl` remain internal helpers.  

**Watch out:**  
- Duplicate‑page filtering strips all links, URLs, and whitespace before hashing; distinct pages that share the same textual core may be unintentionally omitted.  
- The crawl stops as soon as `maxPages` is reached or `stop()` is called, potentially leaving pending URLs unprocessed.  
- `runGeneralCrawl` may perform a bootstrap discovery from the site root if the initial entry yields no pages; this extra discovery can introduce URLs outside the original scope if `scopePrefix` is not set.  
- Consumers must iterate the returned async iterator (or at least await `result`) to allow the cleanup logic (saving the run and closing the browser) to execute; neglecting to do so may leave the browser process running.

---

### `src/lib/layout.mjs`
**Role:** This file owns the complete layout decision process for a crawl, turning raw page markdown into one or more `.md` files according to the task‑driven grouping rules. It defines how sections are split, how filenames are sanitized and derived, and how front‑matter is generated, then assembles the final file objects.

**Key exports:**  
`planFiles({ model, task, pages })` – async function that returns a `Promise` resolving to an array of objects `{ filename, title, markdown, bytes, pages: string[] }` describing the output files.

**Non-obvious deps:** none

**Side effects:** none

**Architecture:**  
- Imports `slug` from `./url.mjs` and `aiPlanLayout` from `../engine/decide.mjs`.  
- Does not create or modify any module‑level globals; all state lives in local variables or arguments.  
- Exposes `planFiles` as the stable public API; all other functions (`splitSections`, `sanitizeName`, `taskToName`, `pageFileBase`, `deriveTitle`, `frontMatter`, `makeFile`, `singleFile`, `filesPerPage`) are internal helpers used only by `planFiles`.

**Watch out:**  
- `sanitizeName` mutates the supplied `Set` of used names, so callers must provide a fresh set for each planning run.  
- `planFiles` assumes each entry in `pages` contains a non‑empty `markdown` string, a `title`, and a `url`; missing fields will cause the page to be omitted from the output.  
- When the total number of units exceeds `UNIT_CAP` (150), the function bypasses the AI model and falls back to a single consolidated file.  
- The AI model’s response must contain either `perPage: true` or a `files` array with `filename` and numeric `items`; any deviation triggers the fallback logic.  
- Leftover units are merged into the largest existing group unless they outnumber that group, in which case a separate “other.md” file is created.

---

### `src/lib/output.mjs`
**Role:** Manages the final disk persistence step by writing a pre‑assembled list of Markdown files and a manifest.json into a target directory, ensuring the directory exists beforehand.  

**Key exports:**  
- `writeBundle(dir, { files, manifest })` — asynchronously creates `dir`, writes each `filename`/`markdown` pair as a plain `.md` file, writes `manifest.json`, and resolves to `{ dir, manifestPath, files }`.  

**Non-obvious deps:** none  

**Side effects:** Creates the resolved `dir` on the filesystem, writes one file per entry in `files` (overwriting any existing file with the same basename), and writes `manifest.json` with a trailing newline.  

**Architecture:** Imports `mkdir` and `writeFile` from `node:fs/promises` and `path` from `node:path`; does not import any project modules. No global state is initialized. The sole export `writeBundle` is the stable public API; there are no internal helper exports.  

**Watch out:** Filenames are sanitized with `path.basename`, so any directory components in `filename` are stripped before writing; callers must ensure each `filename` is unique after basename extraction to avoid accidental overwrites. The function overwrites existing files without warning, and the manifest must be JSON‑serializable because it is stringified directly.

---

### `src/lib/runs.mjs`
**Role:** Manages the persistent cache for each crawl run, assigning a unique run identifier, storing the generated Markdown files and manifest, and providing CRUD operations for the cached runs.  

**Key exports:**  
- `cacheRoot(opts = {}): string` – resolves the cache directory using an explicit option, the `DOCDNA_CACHE_DIR` env var, or the project default.  
- `newRunId(d = new Date()): string` – returns a timestamp‑based, collision‑resistant identifier such as `20260615-084021-3f9c1a`.  
- `saveRun({targets, options, result, files = []}): Promise<{id, dir, manifest, summary}>` – writes a new run folder with bundled Markdown, `manifest.json`, and `run.json`, then returns metadata.  
- `listRuns(opts = {}): Promise<Array<summary>>` – reads all run directories under the cache root, parses each `run.json`, and returns the summaries sorted newest‑first.  
- `getRun(id, opts = {}): Promise<{id, dir, manifest}>` – loads and parses the `manifest.json` for the specified run.  
- `readRunFile(id, name, opts = {}): Promise<string>` – returns the raw Markdown contents of a named output file from a run.  
- `deleteRun(id, opts = {}): Promise<true>` – removes the entire directory for the given run identifier.  
- `deleteAllRuns(opts = {}): Promise<number>` – deletes every cached run directory and resolves with the count of removed runs.  

**Non-obvious deps:** none  

**Side effects:**  
- `saveRun` creates a new directory under the resolved cache root, writes a bundled collection of Markdown files, a `manifest.json`, and a `run.json`.  
- `deleteRun` recursively deletes the run’s directory from the filesystem.  
- `deleteAllRuns` iterates over all cached runs and invokes `deleteRun` for each, causing multiple filesystem deletions.  
- `listRuns` performs directory reads and file parses to assemble run summaries.  

**Architecture:**  
- Imports `writeBundle` from `./output.mjs`.  
- Computes `PROJECT_ROOT` from the module URL and uses it to derive the default cache location.  
- Owns the regular expression `ID_RE` for validating run identifiers.  
- Exposes `cacheRoot`, `newRunId`, `saveRun`, `listRuns`, `getRun`, `readRunFile`, `deleteRun`, and `deleteAllRuns` as the stable public API; internal helpers `runDir`, `sanitizeOptions`, and `buildManifest` remain private.  

**Watch out:**  
- `runDir` throws if the supplied `id` does not match `/^[A-Za-z0-9_-]+$/`; callers must use identifiers produced by `newRunId` or otherwise conform to the pattern.  
- `saveRun` determines the target directory from `options.cacheDir`, then falls back to `process.env.DOCDNA_CACHE_DIR`, and finally to the project‑relative default; an unexpected override can redirect runs to an unintended location.  
- `sanitizeOptions` removes any function values from `options` and serializes regular expressions to their source strings, so callbacks passed in `options` are omitted from the persisted manifest.  
- `deleteAllRuns` relies on the snapshot returned by `listRuns`; concurrent additions or deletions of run directories may cause the returned count to differ from the actual number of filesystem removals.

---

### `src/lib/url.mjs`
**Role:** This file owns all URL‑related normalization, comparison, and slug generation logic used throughout the crawler. It provides pure, dependency‑free helpers that other modules call to decide link deduplication and crawl scope.

**Key exports:**
- `toRegExp(pattern)` — returns a `RegExp` built from the pattern or `null` if the pattern cannot be compiled.
- `normalizeUrl(input, base)` — returns a normalized URL string (lower‑cased host, stripped tracking query params, trailing‑slash removed except for root) or `null` when parsing fails or the protocol is not HTTP/HTTPS.
- `hostOf(u)` — returns the lower‑cased hostname of `u` or an empty string on error.
- `originOf(u)` — returns the origin part of `u` or an empty string on error.
- `pathOf(u)` — returns the pathname of `u` or an empty string on error.
- `sameSite(url, baseUrl)` — returns `true` when the host of `url` equals or is a subdomain of the host of `baseUrl`.
- `inScope(url, baseUrl, { include, exclude } = {})` — returns a boolean indicating whether `url` is within crawl scope according to exclude, include, and same‑site rules.
- `resolveUrl(href, base)` — returns the result of `normalizeUrl` for the href against the base or `null` if invalid.
- `slug(text)` — returns a filesystem‑safe slug derived from `text`, lower‑cased, non‑word characters collapsed to hyphens, trimmed, limited to 80 characters, or `"section"` when the result would be empty.

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** 
- Imports: none  
- Global state: none  
- All listed exports are part of the stable public API; there are no internal helper exports.

**Watch out:** 
- `normalizeUrl` removes any query parameter whose name appears in `STRIP_PARAMS`; callers must not expect those parameters to survive normalization.  
- `normalizeUrl` strips a trailing slash only when the pathname length exceeds one character, preserving the slash for root URLs.  
- `sameSite` treats a host that ends with `.` + base host as a subdomain but does not treat the base domain itself as a parent.  
- `inScope` evaluates `exclude` before `include`; a URL matching both patterns will be excluded.  
- `slug` collapses consecutive non‑word characters into a single hyphen, trims leading/trailing hyphens, and falls back to `"section"` when the processed text is empty.

---

### `src/profiles/docs.mjs`
**Role:** This file implements the documentation‑specific crawl profile that decides which source of page URLs to use (llms‑full.txt, sitemap.xml, or live engine) and orchestrates the handling of each discovered page through the crawl context. It encapsulates the tiered strategy logic that prioritises completeness for documentation tasks.

**Key exports:**
- `runDocsProfile(target, ctx)` — async function that returns a `Promise<void>`; selects the appropriate tier (llms‑full, sitemap, or engine), emits strategy and discovery events on `ctx`, updates totals, adds pages, and finally invokes `ctx.runEngine` with the proper options.

**Non-obvious deps:** 
- `tryLlmsFull` from `./docs/llms.mjs` (fetches the publisher’s llms‑full.txt export)  
- `collectSitemapUrls` from `./docs/sitemap.mjs` (parses a sitemap.xml into URL list)  
- `normalizeUrl`, `inScope`, `pathOf` from `../lib/url.mjs` (URL normalization, scoping, and path extraction)

**Side effects:** 
- May perform network requests via `tryLlmsFull` and `collectSitemapUrls`  
- Emits events (`strategy`, `discover`, `warn`) on the supplied `ctx` object  
- Mutates `ctx` state (`setTotal`, `addPage`, `markProcessed`)  
- Triggers the crawl engine, which launches a Playwright browser instance

**Architecture:** 
- Direct imports: `tryLlmsFull`, `collectSitemapUrls`, `normalizeUrl`, `inScope`, `pathOf`  
- No global singletons are instantiated here  
- Public API: `runDocsProfile` (stable export)  
- Internal helpers: `scopePrefixFor`, `filterDocUrls` (used only within this module)

**Watch out:** 
- The sitemap strategy is chosen only when the filtered seed list contains **more than one** URL; a single‑page sitemap falls back to the engine crawl.  
- If the base URL lacks a path segment, `scopePrefixFor` returns `null`, causing `filterDocUrls` to skip prefix filtering and potentially broaden the crawl scope.  
- The function checks `ctx.shouldStop` at several points; an early stop can leave `ctx.total` unset and may abort processing before any pages are added.

---

### `ui/server.mjs`
**Role:** It runs the minimal Node http server that delivers the UI HTML page, accepts `/start` and `/stop` commands to control a single active `crawlDocs` run, streams core events over `/events` with Server‑Sent Events, and provides a small REST surface (`/runs`, `/runs/:id`, `/runs/:id/file`) for reading and deleting cached crawl results.  

**Key exports:**  
- `startServer({ port = 4000 } = {})` – asynchronously creates and starts the HTTP server, returns `{ server, port }`.  

**Non-obvious deps:**  
- `ensureBrowser` (from `src/lib/browser.mjs`) – internally starts Playwright and expects a Chromium binary; the server prints a warning if Chromium is missing.  

**Side effects:**  
- Listening on the chosen TCP port creates a network socket.  
- Writes status messages to `process.stdout` and error diagnostics to `process.stderr`.  
- Calls `ensureBrowser` which may launch a Chromium process.  
- Maintains the global `clients` Set, `currentRun` reference, and `eventBuffer` array, which are mutated as requests are handled.  

**Architecture:**  
- Imports project modules: `crawlDocs`, `ensureBrowser`, `listRuns`, `getRun`, `readRunFile`, `deleteRun`, `deleteAllRuns`, `cacheRoot`.  
- Owns the mutable singletons `clients`, `currentRun`, and `eventBuffer`.  
- Exposes only `startServer` as the stable public API; all request‑handling functions (`broadcast`, `readBody`, `json`, `handleStart`, `handleEvents`, `handleRunsList`, `handleRunGet`, `handleRunFile`, `handleRunDelete`, `handleRunsClear`, `handleIndex`) remain internal.  

**Watch out:**  
- Only one crawl may run at a time; `handleStart` forcibly stops any existing `currentRun` before launching a new one.  
- The request body is limited to 5 MiB; larger payloads trigger a 400 response and connection termination.  
- `eventBuffer` caps at 5 000 entries; older events are dropped silently.  
- SSE clients are removed on write errors, but a lingering client may keep the ping interval alive until the connection closes.  
- If the chosen port is already bound, the server aborts with a clear message suggesting an alternate port.  
- `ensureBrowser` can resolve to `false` when Chromium is unavailable, causing crawls to fall back to static mode; the UI prints a warning but continues to operate.

## Configuration

### `package.json`
**Role:** Declares the package metadata for *docdna*, including its name, version, description, licensing, and required Node engine. Defines the module entry point (`src/index.mjs`), the CLI binary (`bin/cli.mjs`), the files to publish, and the scripts used to start or serve the application.

**Key exports:**  
- `docdnaCLI()` — launches the command‑line interface defined in `bin/cli.mjs`; starts a crawl or serves the UI, no return value.  
- `docdnaModule()` — loads the library API from `src/index.mjs`; returns the public functions exported by that module (e.g., `crawlDocs`).

**Non-obvious deps:**  
- `ollama` ^0.5.11 — communicates with a local Ollama AI model for task‑judgment.  
- `playwright` ^1.49.0 (optional) — provides headless browser automation; absence may cause runtime failures when interactive crawling is required.  
- `turndown-plugin-gfm` ^1.0.2 — extends Turndown with GitHub‑Flavored Markdown support, affecting Markdown output.

**Side effects:** none

**Architecture:** Directly references `src/index.mjs` as the main module export and `bin/cli.mjs` as the executable binary. No internal helpers are defined here; the file only maps these entry points. The exports listed above constitute the stable public API (module import) and the CLI command, while the rest of the fields are configuration for packaging and scripts.

**Watch out:** The optional `playwright` dependency must be installed on systems that need interactive crawling; otherwise, calls that rely on it will throw at runtime. The package requires Node >= 20, so using an older Node version will prevent execution.

## Documentation

### `build-spec.md`
**Role:** This file defines the complete build specification for the docdna project, describing the core principles, engine behavior, public API, event model, CLI, UI, output format, dependencies, and implementation notes. It exclusively owns the authoritative reference that guides implementation and documentation of all components.

**Key exports:** none

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** This file does not import any project modules, does not create or own global state, and provides no runtime exports; it serves solely as static documentation.

**Watch out:** none

---

### `README.md`
**Role:** This README provides the primary user‑facing documentation for the docdna project, detailing its purpose, installation steps, CLI commands, Web UI usage, and library API. It also records the expected layout of cached run directories and the structure of the generated `manifest.json` and `run.json` files.

**Key exports:** none

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** none

**Watch out:** none

## Templates

### `ui/index.html`
**Role:** This file implements the complete browser UI for docdna, providing the setup form, history list, live‑run view, and markdown preview pane. It orchestrates user interactions, server communication, and dynamic DOM updates without relying on external JavaScript libraries. All visual styling, event wiring, and state handling for the web UI are contained here.

**Key exports:** none

**Non-obvious deps:** none

**Side effects:** On page load the script registers click handlers for every control element, creates the initial empty link row, and immediately calls `renderHistory()` to populate the runs list. Clicking **Start crawl** creates an `EventSource` to `/events` and begins polling server‑sent events. The **Back** button closes any open `EventSource` and resets UI state. All fetch calls mutate the DOM based on JSON responses from `/runs`, `/start`, `/stop`, and file endpoints.

**Architecture:** The file imports no project modules; it relies solely on native browser APIs (`document`, `fetch`, `EventSource`). It owns the mutable globals `es`, `liveRunId`, `activeFile`, and `rowsEl`. Functions such as `renderMarkdown`, `showView`, `addRow`, `collectTargets`, `collectOptions`, `renderHistory`, `renderFiles`, `loadFile`, `openRun`, `logLine`, `handle`, and `connect` are internal helpers used exclusively by the UI script. No symbols are exported for external consumption.

**Watch out:** The script assumes the backend provides `/runs`, `/start`, `/events`, `/stop`, and `/runs/:id/file` endpoints with the exact JSON shape used in the code; missing fields (e.g., `run.task` or `run.files`) will cause uncaught errors. `renderMarkdown` escapes raw text before inserting it via `innerHTML`; a flaw in `esc` could re‑introduce XSS vectors. An `EventSource` is only closed when the **Back** button is pressed, so navigating away without using that button may leave the connection open. Changing any element ID referenced in the script (e.g., `run-viewer`, `status`) will break the corresponding UI logic.

## Other Files

### `LICENSE`
**Role:** This file contains the full text of the MIT license that governs the docdna project. It defines the permissions, conditions, and limitations for using, modifying, and distributing the software. Packaging tools and distribution scripts reference it to satisfy legal compliance when publishing the code.

**Key exports:** none

**Non-obvious deps:** none

**Side effects:** none

**Architecture:** none

**Watch out:** none

---

### `.gitignore`
**Role:** Specifies the files and directories that Git should ignore for this project, including `node_modules/`, `out/`, `.docdna/`, `*.log`, `.DS_Store`, and `.env`.
**Exports:** none

---
*Generated by [repoDNA](https://github.com/BogdanVasaiu/repodna)*
